{
    "version": "5.3.13.35923969-20fd9da5",
    "description": "An AI-native desktop Agent WorkBench",
    "homepage": "https://www.codebuddy.cn/work/",
    "license": "Proprietary",
    "url": "https://download.codebuddy.cn/workbuddy/saas/win32-x64-user/WorkBuddy-win32-x64-user-5.3.13.35923969-20fd9da5.exe",
    "hash": "6832c4faddf51369ebf37d27a9bbfdbbf9cb7ca73a0e3bb3182ee5cdd35dab51",
    "pre_install": [
        "# 1. 解压外层 NSIS 安装包",
        "$installer = Get-ChildItem $dir -Filter '*.exe' | Select-Object -First 1",
        "if ($installer) { Expand-7zipArchive $installer.FullName $dir -Removal }",
        "",
        "# 2. 提取内部核心包 (如 $PLUGINSDIR/app-64.7z)",
        "$pluginDir = Get-ChildItem $dir -Directory -Force | Where-Object { $_.Name -match 'PLUGINSDIR|_OUTDIR' } | Select-Object -First 1",
        "if ($pluginDir) {",
        "    $archive = Get-ChildItem $pluginDir.FullName -Filter '*.7z' | Select-Object -First 1",
        "    if ($archive) { Expand-7zipArchive $archive.FullName $dir -Removal }",
        "}",
        "",
        "# 3. 清理所有 NSIS 遗留的无用目录 (以 $ 开头的目录，如 $PLUGINSDIR, $R0, $TEMP 等)",
        "Get-ChildItem $dir -Directory -Force | Where-Object { $_.Name -match '^\\$' } | Remove-Item -Recurse -Force"
    ],
    "shortcuts": [
        [
            "WorkBuddy.exe",
            "WorkBuddy"
        ]
    ],
    "checkver": {
        "url": "https://www.codebuddy.cn/v2/update?platform=workbuddy-win32-x64-user",
        "jsonpath": "$.url",
        "regex": "WorkBuddy-win32-x64-user-(?<version>[\\d.]+-[\\w.]+)\\.exe"
    },
    "autoupdate": {
        "url": "https://download.codebuddy.cn/workbuddy/saas/win32-x64-user/WorkBuddy-win32-x64-user-$version.exe"
    }
}
